{{> header}}
<h2>{{title}}</h2>

<div class="row">
<div class="col-md-4">
<h3> Recent Posts </h3>

{{#each collections.posts}}
        <div class='job'>
            <a href="{{this.path}}">{{this.title}}</a>

        </div>
{{/each}}

</div>

{{> footer}}
